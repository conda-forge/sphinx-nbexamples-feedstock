{% set name = "sphinx-nbexamples" %}
{% set version = "0.3.2" %}
{% set sha256 = "99f50cd2d9c5b7738b12d84a5583dcd231510903f05b2cdcdd2d53fd5ca612d8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - sphinx
    - ipython
    - nbconvert
    - pillow
    - jupyter_client
    - ipykernel
    - six
    - pandoc

test:
  imports:
    - sphinx_nbexamples

about:
  home: https://github.com/Chilipp/sphinx-nbexamples
  license: GPL-2.0
  license_family: GPL
  license_file: LICENSE
  summary: 'Create an examples gallery with sphinx from Jupyter Notebooks'

  description: |
    This module creates an example gallery for your documentation out of a
    bunch of jupyter notebooks using nbconvert.
  doc_url: http://sphinx-nbexamples.readthedocs.io/
  dev_url: https://github.com/Chilipp/sphinx-nbexamples

extra:
  recipe-maintainers:
    - Chilipp
