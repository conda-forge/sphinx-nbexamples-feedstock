{% set name = "sphinx-nbexamples" %}
{% set version = "0.3.1" %}
{% set sha256 = "3e1b1fd33cdcd33613b1e9d5543c58c87cd44bb48f4b7afcb7bf8183206a5a88" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1000
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - sphinx
    - ipython
    - nbconvert
    - pillow
    - jupyter_client
    - ipykernel
    - six
    - pandoc

test:
  imports:
    - sphinx_nbexamples

about:
  home: https://github.com/Chilipp/sphinx-nbexamples
  license: GPL-2.0
  license_family: GPL
  license_file: LICENSE
  summary: 'Create an examples gallery with sphinx from Jupyter Notebooks'

  description: |
    This module creates an example gallery for your documentation out of a
    bunch of jupyter notebooks using nbconvert.
  doc_url: http://sphinx-nbexamples.readthedocs.io/
  dev_url: https://github.com/Chilipp/sphinx-nbexamples

extra:
  recipe-maintainers:
    - Chilipp
